# RecoveryOS Step 15 spoken evidence

Use these figures only with the `SIMULATED` label. They are not live merchant revenue.

1. “RecoveryOS starts with a real paid ₹1 Razorpay Test Mode recovery. The batch evidence is separate and simulated.”
2. “This frozen run uses 500 identical failed-payment inputs with seed `20260821`, configuration `925ba5d2`, and 1,500 stored outcomes across no intervention, naive immediate retry, and RecoveryOS.”
3. “The batch puts ₹28,75,582 at risk. No intervention recovers ₹3,33,131. Naive retry recovers ₹5,50,591. RecoveryOS recovers ₹13,34,223, or 45.2%.”
4. “That is ₹7,83,632 incremental simulated recovery over naive retry—not a claim about live merchant revenue.”
5. “RecoveryOS makes 336 recovery attempts, records 79 simulated contacts, and stops or escalates 164 unsafe cases, including 63 escalations. It shows 51 false-intervention flags versus 59 for naive retry.”
6. “Every aggregate reconciles to the stored outcomes. Re-running the frozen input produces the same result, and the dashboard and About page expose the same values.”
